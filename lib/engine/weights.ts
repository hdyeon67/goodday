// 날짜 점수 가중치 (조정 가능한 상수)
//
// 최종 점수(0~100) = 오행 40% + 지지 25% + 전통택일 20% + 목적보정 15%.
// 값을 바꾸면 전체 추천 결과가 달라지므로 한 곳에서만 관리한다.

export const WEIGHTS = {
  /** (a) 오행 상생·상극: 사용자 일간 오행 vs 날짜 일진(천간) 오행 */
  ohaeng: 0.4,
  /** (b) 지지 관계: 사용자 띠 vs 날짜 지지 (삼합·육합·충·형) */
  jiji: 0.25,
  /** (c) 전통 택일: 손없는 날 등 (목적에 따라 반영 강도 상이) */
  tradition: 0.2,
  /** (d) 목적별 보정: 유리한 오행·요일 매트릭스 */
  purpose: 0.15,
} as const;

/** 시드(이름+생년월일+날짜) 기반 최종 점수 미세 변동 폭 (±2점) */
export const JITTER_RANGE = 2;

/** "피하면 좋은 날"로 분류하는 점수 상한 (이하일 때 후보) */
export const AVOID_THRESHOLD = 45;

/** 추천 기본 조회 일수 */
export const DEFAULT_HORIZON_DAYS = 30;
