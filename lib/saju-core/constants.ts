// 만세력/오행 계산 상수 (검증된 값 — 임의로 바꾸지 말 것)
//
// 케미체크 lib/scoring 의 saju.ts / zodiac.ts / ohaeng.ts 에 흩어져 있던 상수를
// 한 곳으로 모으고, 좋은날에 필요한 지지→오행(JIJI_OHAENG) 매핑을 추가했다.

import type { Cheongan, Jiji, Ohaeng } from "./types";

/** 천간 10종 (甲乙丙丁戊己庚辛壬癸) */
export const CHEONGAN: Cheongan[] = [
  "갑", "을", "병", "정", "무", "기", "경", "신", "임", "계",
];

/** 12지지 순서 (자=0) */
export const JIJI: Jiji[] = [
  "자", "축", "인", "묘", "진", "사", "오", "미", "신", "유", "술", "해",
];

/** 오행 인덱스 순서 (목=0) */
export const OHAENG_ORDER: Ohaeng[] = ["목", "화", "토", "금", "수"];

/** 천간 → 오행 (갑을=목, 병정=화, 무기=토, 경신=금, 임계=수) */
export const CHEONGAN_OHAENG: Ohaeng[] = [
  "목", "목", "화", "화", "토", "토", "금", "금", "수", "수",
];

/**
 * 지지 → 오행 (JIJI 순서와 인덱스 정렬).
 *   인·묘 = 목, 사·오 = 화, 진·술·축·미 = 토, 신·유 = 금, 해·자 = 수
 *   [자, 축, 인, 묘, 진, 사, 오, 미, 신, 유, 술, 해]
 */
export const JIJI_OHAENG: Ohaeng[] = [
  "수", // 자
  "토", // 축
  "목", // 인
  "목", // 묘
  "토", // 진
  "화", // 사
  "화", // 오
  "토", // 미
  "금", // 신
  "금", // 유
  "토", // 술
  "수", // 해
];

/** 1900-01-01 의 60갑자 index (甲戌 = 10) — 검증된 앵커 */
export const BASE_GAPJA_INDEX = 10;
/** 1900-01-01 00:00 UTC 타임스탬프 (타임존 비결정성 제거를 위해 UTC 사용) */
export const BASE_UTC = Date.UTC(1900, 0, 1);
export const MS_PER_DAY = 24 * 60 * 60 * 1000;

// ── 지지 관계 ──────────────────────────────────────────────

/** 삼합(三合): 같은 局 3지지끼리 강한 상성 (JIJI 인덱스 기준) */
export const SAMHAP: number[][] = [
  [8, 0, 4], // 신자진 수국
  [2, 6, 10], // 인오술 화국
  [5, 9, 1], // 사유축 금국
  [11, 3, 7], // 해묘미 목국
];

/** 육합(六合): 짝 상성 */
export const YUKHAP: [number, number][] = [
  [0, 1], [2, 11], [3, 10], [4, 9], [5, 8], [6, 7],
];

/**
 * 형(刑): 예의를 잃는 관계. 전통 택일에서 가벼운 감점 요소로 쓴다.
 *   삼형: 인사신 / 축술미, 상형: 자묘, 자형: 진진·오오·유유·해해
 * (자형은 동일 지지끼리이므로 별도 판정에서 처리)
 */
export const HYEONG: [number, number][] = [
  // 인사신 삼형
  [2, 5], [5, 8], [2, 8],
  // 축술미 삼형
  [1, 10], [10, 7], [1, 7],
  // 자묘 상형
  [0, 3],
];

/** 자형(自刑): 같은 지지끼리 형이 성립하는 지지 index (진·오·유·해) */
export const JAHYEONG: number[] = [4, 6, 9, 11];
